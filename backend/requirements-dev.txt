# DentNow backend development/test requirements (installed on top of requirements.txt).

pytest>=8.0.0
pytest-cov>=5.0.0

# Lint / format / types
ruff>=0.6.0
mypy>=1.10.0

# Type stubs
types-requests>=2.31.0
types-bleach>=6.1.0
boto3-stubs[s3]>=1.34.0

# Test helpers (in-test RSA/JWKS for token verification, HTTP stubbing)
cryptography>=42.0.0
responses>=0.25.0
